# v5 修改说明（2026-07-18）

输出：`C:\Users\12092\Desktop\论文_修改版v5_期刊版式.docx`（基于 v4；共 157 处正文编辑 + 8 张表全部重构）。

## 1. 口语化二轮清理（约 30 处）

清除疑问代词式陈述与残留口语："哪些环节失效、哪些环节有效"→"失效环节与有效环节"、"高AUC来自哪里"→"高AUC的来源"、"什么能做、什么不能做"→"可行与不可行"、"怎么表述"→"允许与禁止的表述方式"、"其实是"→"实为"、"跑通/跑4个配置/上跑"→"执行/运行"、"打分到"→"给出的得分"等。研究问题（RQ）保留问句形式但措辞正规化（"边界在哪里"→"边界位于何处"）。终检"哪些/哪里/到底/其实/跑"全文零残留。

## 2. 术语中文释义（约 45 处首现配注 + 全文统一）

- **全文改名**：pooledAUC → **合并AUC**（首现注"pooled AUC,即合并全部辖区测试样本计算的受试者工作特征曲线下面积"）；transfer → **迁移情境**；familyhard-negativeAUC → **家族困难负样本AUC**
- **首现括注**（英文术语保留、后附中文）：DANN(域对抗神经网络)、StruRW(结构重加权)、IRM(不变风险最小化)、EERM(探索-外推风险最小化)、source_only(仅源域训练基线)、finetune(微调)、hard-negative(困难负样本)、Youden threshold(约登阈值)、Holm(霍尔姆校正)、bootstrap(自助重采样)、trivial classifier(平凡分类器)、group-aware split(分组感知划分)、metapath(元路径)、InfoNCE(噪声对比估计)、frozen linear probe(冻结编码器线性探针)、transductive(直推式)、inductive(归纳式)、leave-one-family-out(留一家族)、OOD(分布外)、CSBM(上下文随机块模型)、SupCon(监督对比学习)、ccTLD(国家代码顶级域名)、seed(随机种子)、shot(每类校准样本数)、benchmark(基准数据集)、head(分类头)、encoder(编码器)、embedding(嵌入)、dHash(差值哈希)、t-SNE、mean_pred_illegal(目标域非法预测概率均值)等
- **术语修正**：CSBM 译名统一为"上下文随机块模型"（原 §2.3 误作"条件随机块模型"）
- 英文摘要、参考文献、附录C 命令块不做汉化（规范如此）

## 3. 符号改中文（约 35 处）

- "→"："0.89→0.93"→"从0.89提升至0.93"、"operator_or_case→brand→root_domain"→顿号列举、"[64→64→2]"→"各层维度依次为64、64、2"、"prefit_1→prefit_2→final"→"依次进行"
- "≥/>/<"："≥10shot"→"不少于10个校准样本"、"Δ>+0.05"→"Δ大于+0.05"、"effect<0"→"效应值小于0"、"某指标≥某阈值"→"不低于"
- "vs"→"与/对"（含 §5.1 标题、图6(a)/图7 题注）；"~"→"约"
- **保留**：公式(1)–(9) 内数学符号、括号内统计规范表达（p<0.001、FWER≤0.05）、§3.2 边类型定义行的 schema 记号 W→IP（引导句已注明记号含义）、文件名中的 "_vs_"

## 4. 表格重构（8 张全部单行化）

- 全部改为**固定布局 + 按内容实测重分配列宽 + 单元格禁止折行(noWrap)**，表内字号统一 8pt
- 超长文本缩短并将全称移入**表注**（新增 7 条 8pt 表注）：表1 标签层改中文短名（内部字段名入注）、表2 方法列中文化（逻辑回归/多层感知机/HeCo微调等）、表3/表4/表5 表头中文化（"迁移情境/源域/目标类别/主指标"）、表8 列名中文化（困难负样本AUC/分数间隔）
- 逐单元格宽度校验（中文字符 160 twips、ASCII 85 估算）：**0 个越界单元格**

## 验证汇总

空格哨兵通过（v4 输入完好）；公式 9 个完好；总块数 347（332 段 + 8 表 + 7 表注）；残留扫描（哪些/哪里/到底/其实/跑/vs/→/≥/pooledAUC）全部为零（括注内英文原词 "(illegal vs licensed)" 属释义保留）；19 项释义关键词全部在位。

版本链：v1 → v2（审稿修改）→ v3（去口语+重绘图）→ v4（+公式）→ **v5（术语/符号/表格规范化，当前投稿候选）**。
